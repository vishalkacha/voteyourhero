# Vote Your Hero

A simple core PHP registration form.

## Run locally

```bash
php -S 127.0.0.1:8000
```

Then open `http://127.0.0.1:8000` in your browser.

Submitted registrations are saved to `data/users.json`, which is ignored by Git.
